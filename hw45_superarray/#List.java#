public interface List {
    //returns true
    public boolean add(Object x);

    public Object get(int index);
    //returns the element in tis list whose position is the argument
    // throws indexOutOFBounds exception if index < 0 || index >= size()

    public Object set(int index, Object o);
    // returns the element previously stored at the position
    // throws indexOutOFBounds exception if index < 0 || index >= size()
    